<script lang="ts">let { data, form } = $props();</script>
<main class="auth-page"><section class="auth-card"><div class="brand"><span class="brand-mark">L</span> Levity</div><h1>Seu espaço de trabalho</h1>{#if data.empty}<p class="muted">Crie o primeiro workspace. Um board e as quatro colunas padrão serão criados pela API.</p><form method="POST" action="?/createWorkspace"><label class="field">Nome do workspace <input name="name" maxlength="100" required /></label>{#if form?.error}<p class="auth-error" role="alert">{form.error}</p>{/if}<button class="auth-submit">Criar workspace</button></form>{/if}</section></main>
