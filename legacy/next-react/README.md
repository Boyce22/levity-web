# Arquivo Next.js/React

Este diretório preserva a implementação anterior do frontend, retirada da árvore ativa em 2026-09-14.

Ela não é importada pelo SvelteKit e fica excluída de build, typecheck e lint. Use-a somente como referência durante a migração; código novo deve ficar em `src/routes` ou `src/lib`.
